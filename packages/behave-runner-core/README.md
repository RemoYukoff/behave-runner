# @behave-runner/core

Language logic aligned with **Behave** (not full standard Gherkin): feature parsing, step patterns, and shared types.

Intended consumers:

- The VS Code extension (`behave-runner`).
- A future **Language Server** can depend on this package to reuse the same analysis without duplicating rules.
